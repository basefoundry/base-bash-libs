# Apply with: shfmt --language-dialect bash --indent 4 --space-redirects
# Formatting is opt-in; the project kit never replaces ShellCheck or shfmt.
language-dialect = bash
indent = 4
space-redirects = true
